{% extends 'base.html.twig' %}

{% block title %}DestinationCity{% endblock %}

{% block body %}
    <h1>DestinationCity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ destination_city.id }}</td>
            </tr>
            <tr>
                <th>DestinationCity</th>
                <td>{{ destination_city.destinationCity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('destination_city_index') }}">back to list</a>

    <a href="{{ path('destination_city_edit', {'id': destination_city.id}) }}">edit</a>

    {{ include('destination_city/_delete_form.html.twig') }}
{% endblock %}

{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(form) }}
    {# bootstrap calss for styling the button #}
    <input type="submit" value="Save" class="btn btn-success" />
{{ form_end(form) }}
